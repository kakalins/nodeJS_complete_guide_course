const logger = require('./logger'); //const evita que o compilador permita uma nova atribuição à variavel

//console.log(logger);

//logger.log('Ricardo');

logger('Ricardo'); //após exportar o módulo todo (módulo de apenas 1 função)